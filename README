This is fork of ICIR's git-notifier.

Main differences:

* --altsubject support (Software repository in subject)
* --colordiff support (Uses Pygments library)
* --smtpserver support (Uses smtplib)


Please see README.orig for more detail.
